<template name="item">
  <view class='imgs-item'>
    <image src='{{thumb}}' mode='widthFix' bindtap='showImage' data-src="{{middle}}"></image>
    <image class='download-btn' src='/images/down.png' bindtap='download' data-src="{{middle}}"></image>
  </view>
</template>